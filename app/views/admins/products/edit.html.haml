.rounded-sm.mx-auto.col.w-25.border.border-dark
  %h2.text-center
    商品情報の変更
  .container
    = simple_form_for [:admins, @product] do |f|
      = f.input :name, input_html: { value: @product.name }
      = f.input :description, input_html: { value: @product.description }
      = f.input :price, input_html: { value: @product.price }
      = f.input :image, as: :file, input_html: {accept: ("image/jpg,image/jpeg,image/gif,image/png"),value: @product.image}
      .mt-20.text-center
        = f.button :submit